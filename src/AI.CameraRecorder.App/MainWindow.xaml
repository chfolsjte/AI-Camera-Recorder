<Window
    x:Class="AI.CameraRecorder.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AI Camera Recorder">
  <Grid Padding="20" RowSpacing="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Spacing="10">
      <ComboBox x:Name="CameraBox" Width="340" PlaceholderText="USB Camera" DisplayMemberPath="DisplayName"/>
      <Button Content="重新掃描" Click="RefreshCameras_Click"/>
      <ComboBox x:Name="ResolutionBox" Width="150" SelectedIndex="0">
        <x:String>1920x1080</x:String><x:String>2560x1440</x:String><x:String>3840x2160</x:String>
      </ComboBox>
      <ComboBox x:Name="FpsBox" Width="90" SelectedIndex="0">
        <x:String>30</x:String><x:String>60</x:String>
      </ComboBox>
    </StackPanel>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
      <Button x:Name="StartButton" Content="開始錄影" Click="StartRecording_Click"/>
      <Button x:Name="StopButton" Content="停止錄影" Click="StopRecording_Click" IsEnabled="False"/>
      <Button Content="加入既有影片" Click="AddVideo_Click"/>
      <ComboBox x:Name="CodecBox" Width="130" SelectedIndex="0">
        <x:String>H.265</x:String><x:String>AV1</x:String>
      </ComboBox>
      <CheckBox x:Name="DenoiseCheck" Content="AI 去噪" IsChecked="True"/>
      <CheckBox x:Name="SharpenCheck" Content="AI 銳化" IsChecked="True"/>
      <CheckBox x:Name="FaceCheck" Content="GFPGAN 人臉修復"/>
    </StackPanel>

    <StackPanel Grid.Row="2" Spacing="6">
      <TextBlock x:Name="StatusText" Text="待命" FontWeight="SemiBold"/>
      <ProgressBar x:Name="Progress" Minimum="0" Maximum="100"/>
    </StackPanel>

    <Grid Grid.Row="3" ColumnSpacing="14">
      <Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12">
        <ListView x:Name="JobsList" ItemsSource="{x:Bind Jobs}">
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid ColumnSpacing="10" Padding="4">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="120"/><ColumnDefinition Width="90"/></Grid.ColumnDefinitions>
                <TextBlock Text="{Binding SourcePath}" TextTrimming="CharacterEllipsis"/>
                <TextBlock Grid.Column="1" Text="{Binding State}"/>
                <TextBlock Grid.Column="2" Text="{Binding ProgressText}"/>
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Border>
      <Border Grid.Column="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Padding="12">
        <StackPanel Spacing="8">
          <TextBlock Text="執行環境" FontSize="18" FontWeight="SemiBold"/>
          <TextBlock x:Name="FfmpegStatus" Text="FFmpeg：檢查中"/>
          <TextBlock x:Name="GpuStatus" Text="NVIDIA GPU：檢查中"/>
          <TextBlock x:Name="AiStatus" Text="AI 模型：檢查中"/>
          <TextBlock TextWrapping="Wrap" Text="正式發行版會將 FFmpeg、ONNX/TensorRT runtime 與模型放在應用程式 tools/models 資料夾，不需要 Python 或 PowerShell。"/>
        </StackPanel>
      </Border>
    </Grid>
  </Grid>
</Window>
